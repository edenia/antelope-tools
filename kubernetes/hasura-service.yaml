apiVersion: v1
kind: Service
metadata:
  labels:
    app: monitor-hasura
  name: monitor-hasura
spec:
  ports:
  - port: 8080
    targetPort: 8080
    name: hasura
    protocol: TCP
  selector:
    app: monitor-hasura
