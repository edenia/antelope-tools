export default (theme) => ({
  field: {
    marginBottom: theme.spacing(2),
    width: '100%'
  },
  alert: {
    marginBottom: theme.spacing(2)
  },
  title: {
    fontSize: 17,
    fontWeight: '600',
    lineHeight: '1.2',
    letterSpacing: '0.06px',
    textAlign: 'left',
    marginBottom: theme.spacing(2)
  }
})
