export default (theme) => ({
  container: {
    display: 'flex',
    marginBottom: theme.spacing(2),
    gap: theme.spacing(2),
    flexWrap: 'wrap',
  },
  chartContainer: {
    marginBottom: theme.spacing(2),
  },
})
