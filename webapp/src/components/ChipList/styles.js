export default (theme) => ({
  chipsContainer: {
    display: 'flex',
    flexFlow: 'row wrap',
    maxWidth: '300px',
  },
  chip: {
    margin: theme.spacing(0.5),
  },
  bold: {
    fontWeight: 'bold',
  },
})
