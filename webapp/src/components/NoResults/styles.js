export default (theme) => ({
  root: {
    display: 'flex',
    flexDirection: 'column',
    width: '100%',
    marginBottom: theme.spacing(2),
    padding: theme.spacing(2),
    alignItems: 'center',
  },
})
