table:
  name: endpoints_by_producer_id
  schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - type
        - value
        - producer_id
        - response
        - head_block_time
        - updated_at
      filter: {}
      allow_aggregations: true
