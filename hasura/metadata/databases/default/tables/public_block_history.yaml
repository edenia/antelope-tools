table:
  schema: public
  name: block_history
select_permissions:
  - role: guest
    permission:
      columns:
        - id
        - block_id
        - block_num
        - transactions_length
        - timestamp
        - created_at
        - updated_at
        - producer
        - schedule_version
      filter: {}
