table:
  name: block_history
  schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - block_num
        - timestamp
        - transactions_length
        - producer
        - cpu_usage
        - net_usage
        - schedule_version
        - created_at
      filter: {}
