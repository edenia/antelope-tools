CREATE TABLE "public"."block_history" ("block_num" integer NOT NULL, "timestamp" timestamp with time zone NOT NULL, "transactions_length" int2 NOT NULL, "producer" character varying(12) NOT NULL, "cpu_usage" Numeric(5,2) NOT NULL, "net_usage" Numeric(6,3) NOT NULL, "schedule_version" integer NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("block_num") , UNIQUE ("block_num"));
